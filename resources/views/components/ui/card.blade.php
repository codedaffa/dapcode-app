@props([ 'title' => null, 'subtitle' => null, 'icon' => null, 'hoverable' => false, 'compact' => false, 'flush' => false, ]) @php $cardClasses = [ 'ui-card', $hoverable ? 'ui-card-hoverable' : '', ]; $cardClasses = trim(implode(' ', array_filter($cardClasses))); $bodyClasses = [ 'ui-card-body', $compact ? 'ui-card-body-compact' : '', $flush ? 'ui-card-body-flush' : '', ]; $bodyClasses = trim(implode(' ', array_filter($bodyClasses))); @endphp <div {{ $attributes->merge(['class' => $cardClasses]) }}> @if($title || isset($header) || isset($actions)) <div class="ui-card-header"> @if(isset($header)) {{ $header }} @else <div class="ui-card-header-left"> @if($icon) <div class="ui-card-icon"><i class="{{ $icon }}"></i></div> @endif <div class="ui-card-title-group"><h3 class="ui-card-title">{{ $title }}</h3> @if($subtitle) <p class="ui-card-subtitle">{{ $subtitle }}</p> @endif </div></div> @endif @if(isset($actions)) <div class="ui-card-actions">{{ $actions }}</div> @endif </div> @endif <div class="{{ $bodyClasses }}">{{ $slot }}</div> @if(isset($footer)) <div class="ui-card-footer">{{ $footer }}</div> @endif </div>