@props([ 'action' => null, 'method' => 'GET', 'resetUrl' => null, ]) <div {{ $attributes->merge(['class' => 'ui-card', 'style' => 'margin-bottom: 18px;']) }}><div class="ui-card-body" style="padding: 14px 18px;"><form @if($action) action="{{ $action }}" @endif method="{{ $method }}" style="display: flex; align-items: flex-end; gap: 12px; flex-wrap: wrap; margin-bottom: 0;"><div style="display: flex; gap: 12px; flex: 1; flex-wrap: wrap; align-items: flex-end;">{{ $slot }}</div><div style="display: flex; gap: 8px; align-items: center;"><x-ui.button type="submit" variant="primary" icon="fa-solid fa-filter"> Filter </x-ui.button> @if($resetUrl) <x-ui.button :href="$resetUrl" variant="ghost" icon="fa-solid fa-rotate-left"> Reset </x-ui.button> @endif </div></form></div></div>