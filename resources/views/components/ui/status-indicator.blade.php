@props([ 'status' => 'active', 'label' => null, 'pulse' => false, ]) @php $dotClasses = [ 'ui-status-dot', 'is-' . $status, $pulse ? 'is-pulse' : '', ]; $dotClasses = trim(implode(' ', array_filter($dotClasses))); @endphp <div {{ $attributes->merge(['class' => 'ui-status-indicator']) }}><span class="{{ $dotClasses }}"></span> @if($label || $slot->isNotEmpty()) <span>{{ $label ?? $slot }}</span> @endif </div>