@props([ 'name' => null, 'id' => null, 'label' => null, 'options' => [], 'selected' => null, 'placeholder' => null, 'help' => null, 'error' => null, 'required' => false, 'disabled' => false, ]) @php $selectId = $id ?: ($name ?: 'ui-select-' . uniqid()); $hasError = $error || ($name && isset($errors) && $errors->has($name)); $errorMessage = $error ?: ($name && isset($errors) ? $errors->first($name) : null); $currentVal = $selected !== null ? $selected : ($name ? old($name) : null); $selectClasses = [ 'ui-select', $hasError ? 'is-invalid' : '', ]; $selectClasses = trim(implode(' ', array_filter($selectClasses))); @endphp <div class="ui-form-group"> @if($label) <label for="{{ $selectId }}" class="ui-label">{{ $label }} @if($required) <span class="ui-label-required"> * </span> @endif </label> @endif <div class="ui-input-wrapper"><select id="{{ $selectId }}" @if($name) name="{{ $name }}" @endif @if($required) required @endif @if($disabled) disabled @endif {{ $attributes-> merge(['class' => $selectClasses]) }} > @if($placeholder) <option value="" disabled {{ $currentVal === null || $currentVal === '' ? 'selected' : '' }}>{{ $placeholder }}</option> @endif @if(!empty($options) ) @foreach($options as $optValue => $optLabel) <option value="{{ $optValue }}" {{ (string)$currentVal === (string)$optValue ? 'selected' : '' }}>{{ $optLabel }}</option> @endforeach @endif {{ $slot }}</select></div> @if($hasError) <span class="ui-error-text">{{ $errorMessage }}</span> @elseif($help) <span class="ui-help-text">{{ $help }}</span> @endif </div>