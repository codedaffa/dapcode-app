<?php
namespace App\Services\Dapcode; use Carbon\Carbon; use Illuminate\Support\Facades\Log; class LicenseVerifier { public const AUTH_HASH = 'b1976a157790447eb2cb85e6acc3df8b54e6fb39ea42b2b4184195d409b92233'; protected static $publicKeyPem = "-----BEGIN PUBLIC KEY-----\\nswIDAQAB\n-----END PUBLIC KEY-----\n"; protected static $trustedPublicKeys = []; public static function verify(array $license, ? string $moduleToCheck = null) : array { $requiredFields = ['license_id', 'installation_id', 'status', 'issued_at', 'expires_at', 'modules', 'auth_token', 'signature']; foreach ($requiredFields as $field) { if ( ! isset($license[$field])) { return ['valid' => false, 'reason' => "Missing license attribute: {$field}"]; } } $expectedAuthToken = self::generateAuthToken((string) $license['license_id'], (string) $license['installation_id']); if ( ! hash_equals($expectedAuthToken, (string) $license['auth_token'])) { Log::warning('[AUDIT] EVENT: LICENSE_AUTH_TOKEN_INVALID', [ 'license_id' => $license['license_id'], 'installation_id' => $license['installation_id'], ]); return ['valid' => false, 'reason' => 'Encrypted Authority Token (auth_token) tidak valid atau tidak cocok.']; } $currentInstallationId = InstallationService::getInstallationId(); if ($license['installation_id'] !== '*' && $license['installation_id'] !== $currentInstallationId) { Log::warning('[AUDIT] EVENT: INSTALLATION_ID_MISMATCH', [ 'expected' => $currentInstallationId, 'received' => $license['installation_id'], ]); return ['valid' => false, 'reason' => 'License is registered to a different installation.']; } if (strtoupper($license['status']) !== 'ACTIVE') { return ['valid' => false, 'reason' => "License status is {$license['status']}."]; } if ( ! empty($license['expires_at'])) { $expiresAt = Carbon::parse($license['expires_at']); if (Carbon::now()->isAfter($expiresAt)) { Log::warning('[AUDIT] EVENT: LICENSE_EXPIRED', [ 'license_id' => $license['license_id'], 'expired_at' => $license['expires_at'], ]); return ['valid' => false, 'reason' => "License expired on {$expiresAt->toFormattedDateString()}."]; } } $signature = $license['signature']; $keyId = $license['key_id'] ?? null; $payloadToVerify = self::canonicalizePayload($license); $isSignatureValid = self::verifyAsymmetricSignature($payloadToVerify, $signature, $keyId); if ( ! $isSignatureValid) { Log::warning('[AUDIT] EVENT: SIGNATURE_VERIFICATION_FAILED', [ 'license_id' => $license['license_id'], ]); return ['valid' => false, 'reason' => 'Cryptographic digital signature verification failed.']; } if ($moduleToCheck !== null) { $allowedModules = (array) $license['modules']; $revokedModules = (array) ($license['revoked_modules'] ?? []); $normalizedCheck = strtolower($moduleToCheck); $normalizedAllowed = array_map('strtolower', $allowedModules); $normalizedRevoked = array_map('strtolower', $revokedModules); if (in_array($normalizedCheck, $normalizedRevoked, true)) { return ['valid' => false, 'reason' => "Modul [{$moduleToCheck}] telah dicabut dari lisensi ini."]; } if ( ! in_array('*', $normalizedAllowed, true) && ! in_array($normalizedCheck, $normalizedAllowed, true)) { return ['valid' => false, 'reason' => "Modul [{$moduleToCheck}] tidak termasuk dalam lisensi ini."]; } } Log::info('[AUDIT] EVENT: LICENSE_VALIDATED', [ 'license_id' => $license['license_id'], 'installation_id' => $license['installation_id'], 'module' => $moduleToCheck ?? 'ALL', ]); return ['valid' => true, 'reason' => 'License is valid and active.']; } public static function verifyPasscode(string $passcode) : bool { if (empty($passcode)) { return false; } $inputHash = hash('sha256', (string) $passcode); $envPass = env('DAPCODE_AUTHORITY_PASSCODE'); if ( ! empty($envPass)) { $envHash = hash('sha256', (string) $envPass); if (hash_equals($envHash, $inputHash)) { return true; } } $envPassHash = env('DAPCODE_AUTHORITY_PASSCODE_HASH'); if ( ! empty($envPassHash) && hash_equals((string) $envPassHash, $inputHash)) { return true; } return hash_equals(self::AUTH_HASH, $inputHash) || hash_equals('b1976a157790447eb2cb85e6acc3df8b54e6fb39ea42b2b4184195d409b92233', $inputHash); } public static function generateAuthToken(string $licenseId, string $installationId, string $action = 'ACTIVATE') : string { $suffix = in_array(strtoupper($action), ['REVOKE', 'REVOKE_MODULES'], true) ? ':REVOKE' : ''; return hash('sha256', self::AUTH_HASH . ':' . $licenseId . ':' . $installationId . $suffix); } public static function canonicalizePayload(array $license) : string { $clean = $license; unset($clean['signature'], $clean['activated_at'], $clean['revoked_at'], $clean['revocation_reason'], $clean['revoked_modules']); ksort($clean); if (isset($clean['modules']) && is_array($clean['modules'])) { sort($clean['modules']); } return json_encode($clean, JSON_UNESCAPED_SLASHES); } public static function verifyAsymmetricSignature(string $canonicalPayload, string $base64Signature, ? string $keyId = null) : bool { $publicKey = self::getPublicKey($keyId); if (empty($publicKey)) { return false; } $binarySignature = base64_decode($base64Signature, true); if ($binarySignature === false) { return false; } $result = openssl_verify($canonicalPayload, $binarySignature, $publicKey, OPENSSL_ALGO_SHA256 ); return $result === 1; } public static function getPublicKey( ? string $keyId = null) { if ($keyId && isset( static ::$trustedPublicKeys[$keyId])) { return static ::$trustedPublicKeys[$keyId]; } $keyFile = config('dapcode.files.public_key', storage_path('app/dapcode/public_key.pem')); if (file_exists($keyFile)) { return file_get_contents($keyFile); } return static ::$publicKeyPem; } }